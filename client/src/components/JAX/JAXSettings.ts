import { Settings } from "react-chatbotify";

const settings: Settings = {
  isOpen: true,
  chatHistory: {
    disabled: true,
  },
  header: {
    title: "JAX",
    showAvatar: false,
  },
  chatInput: {},
};

export default settings;
